/**
 * GameState represents the current state of the game board
 * GAME_OVER = user lost the game
 * GAME_WON = user won the game
 * GAME_IN_PROGRESS = user has neither won or lost the game
 */
public enum GameState {
  GAME_OVER,
  GAME_WON,
  GAME_IN_PROGRESS
}